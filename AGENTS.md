# 백엔드 작업 지침

이 저장소는 기존 Vue 프로젝트와 연결할 Java 백엔드 학습 프로젝트다.
작업 전에 README.md, docs/IMPLEMENTATION_PLAN.md, docs/DECISIONS.md,
docs/GIT_WORKFLOW.md, docs/QA_CHECKLIST.md를 읽는다.
DB·문의 API 작업 전에는 docs/DATABASE_DESIGN.md와 docs/API_CONTRACT.md도 읽는다.

- 사용자 최신 요구사항을 우선한다. 실행 흐름을 설명할 수 있는 코드를 작성한다.
- 기존 프론트엔드의 API 호출, MSW 핸들러, 타입, Store와 테스트로 계약을 확인한다.
- 계약 변경은 이유와 프론트엔드 영향을 먼저 설명한다.
- 사용자 변경, 프론트엔드 디자인, 기존 학습 문서와 이력서를 보존한다.
- 하나의 Spring Boot 애플리케이션과 PostgreSQL로 시작한다.
- 실제 책임이 생길 때 패키지와 계층을 추가한다. 불필요한 추상화를 만들지 않는다.
- 조회 연결 → 변경 저장 → 인증·권한 → 동시성 검증 순서로 진행한다.
- 공개 배포나 인증 없는 로컬 환경을 상용 보안 수준으로 표현하지 않는다.
- 비밀값과 로컬 환경 설정을 커밋하지 않는다.
- 기능마다 관련 테스트와 실행 방법을 함께 작성한다. 검증하지 않은 결과는 기록하지 않는다.
- study/는 Git에서 제외한 개인 학습 문서다. 실행 예제는 파일별 전체 코드 후 실제 값의 흐름을 설명한다.
- 문서 예제는 관련 코드를 바꾸면 함께 갱신한다.
- 사용자가 브랜치 생성·전환, 커밋·push·PR 생성을 담당한다. 작업 중 현재 브랜치를 변경하지 않는다.
- 작업 후 복사 가능한 커밋 제목·본문과 PR 제목·본문을 사용자에게 전달한다.
- 원격 저장소는 이미 연결되어 있다. 새 원격 생성·공개 범위 변경은 사용자 확인 후 진행한다.
